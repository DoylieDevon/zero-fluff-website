---
phase: 03-content-blog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/content.config.ts
  - src/content/case-studies/apex-logistics.md
  - src/content/case-studies/bright-path-digital.md
  - src/components/CaseStudyCard.astro
  - src/pages/case-studies/index.astro
  - src/pages/case-studies/[id].astro
  - src/components/Header.astro
autonomous: true
requirements: [CASE-01, CASE-02, CASE-03, CASE-04]

must_haves:
  truths:
    - "Visiting /case-studies shows at least 2 project cards with client name and key result"
    - "Clicking a card navigates to /case-studies/{id} showing Problem, Approach, Outcome sections"
    - "Each case study page displays a quantified result prominently"
    - "Each case study page has a CTA at the bottom linking to contact"
    - "Header nav includes a 'Work' link to /case-studies"
  artifacts:
    - path: "src/content/content.config.ts"
      provides: "Content collection schema for case-studies"
      contains: "defineCollection"
    - path: "src/content/case-studies/apex-logistics.md"
      provides: "First case study content"
      contains: "## The Problem"
    - path: "src/content/case-studies/bright-path-digital.md"
      provides: "Second case study content"
      contains: "## The Problem"
    - path: "src/pages/case-studies/index.astro"
      provides: "Case study listing page"
      contains: "getCollection"
    - path: "src/pages/case-studies/[id].astro"
      provides: "Individual case study dynamic route"
      contains: "getStaticPaths"
  key_links:
    - from: "src/pages/case-studies/index.astro"
      to: "src/content/case-studies/*.md"
      via: "getCollection('case-studies')"
      pattern: "getCollection.*case-studies"
    - from: "src/pages/case-studies/[id].astro"
      to: "src/content/case-studies/*.md"
      via: "getStaticPaths + render"
      pattern: "getStaticPaths|render\\(study\\)"
    - from: "src/components/CaseStudyCard.astro"
      to: "/case-studies/{id}"
      via: "href link"
      pattern: "href.*case-studies"
---

<objective>
Create case study listing and individual pages using Astro Content Collections, with 2 real case studies based on existing client references (Apex Logistics, Bright Path Digital).

Purpose: Give visitors concrete evidence of Zero Fluff's results -- the primary trust-building content on the site.
Output: Case study listing page, 2 individual case study pages, content collection config, nav update.
</objective>

<execution_context>
@/Users/andydoyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andydoyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-blog/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/CTA.astro:
```astro
interface Props {
  heading?: string;
  text?: string;
  buttonText?: string;
  buttonHref?: string;
}
```
Defaults: heading="Ready to cut the fluff?", buttonHref="/contact"

From src/components/Header.astro:
```astro
const currentPath = Astro.url.pathname;
<!-- Nav links: Home, Services, About, Contact -->
<!-- Uses class:list with active state based on currentPath -->
```

From src/styles/global.css (utility classes):
```css
.container { max-width: var(--max-width); margin-inline: auto; padding-inline: var(--space-md); }
.narrow { max-width: 42rem; margin-inline: auto; }
.stack > * + * { margin-block-start: var(--space-md); }
.grid-2 { display: grid; gap: var(--space-lg); }
@media (min-width: 768px) { .grid-2 { grid-template-columns: repeat(2, 1fr); } }
.card { background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius); padding: var(--space-lg); }
.reveal { opacity: 0; transform: translateY(1rem); transition: opacity 0.6s ease, transform 0.6s ease; }
.text-muted { color: var(--color-text-muted); }
.text-sm { font-size: var(--text-sm); }
```

Established patterns:
- `export const prerender = true` on all static pages (required -- hybrid mode)
- `BaseLayout` wraps all pages
- `.section` class for page sections with vertical spacing
- `.reveal` class for scroll-triggered fade-in animations
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content collection config and case study markdown files</name>
  <files>src/content/content.config.ts, src/content/case-studies/apex-logistics.md, src/content/case-studies/bright-path-digital.md</files>
  <action>
1. Create `src/content/content.config.ts` defining a `case-studies` collection:
   - Use `glob()` loader with `pattern: '**/*.md'` and `base: './src/content/case-studies'`
   - Schema fields: `title` (string), `client` (string), `result` (string, e.g. "8 hours/week saved"), `order` (number, optional, for sort control)
   - Export as `collections = { 'case-studies': caseStudies }`

2. Create `src/content/case-studies/apex-logistics.md`:
   - Frontmatter: title "Automated Weekly Reporting", client "Apex Logistics", result "8 hours/week saved", order 1
   - Three markdown sections: `## The Problem`, `## The Approach`, `## The Outcome`
   - Content based on homepage testimonial context: operations team spending hours on manual reporting, Zero Fluff built automated pipelines, result was 8 hours/week freed up
   - Write in direct, jargon-free tone matching the site. 2-3 paragraphs per section.
   - Mark content with `<!-- Placeholder: replace with real project details -->` HTML comment at the top for easy swapping

3. Create `src/content/case-studies/bright-path-digital.md`:
   - Frontmatter: title "AI-Powered Customer Insights", client "Bright Path Digital", result "3x faster decision-making", order 2
   - Same three-section structure
   - Content based on homepage testimonial: digital agency struggling with data analysis, Zero Fluff implemented AI tools for customer insights, result was dramatically faster decision cycles
   - Same tone and placeholder comment
  </action>
  <verify>
    <automated>ls -la "/Users/andydoyle/Projects/Zero Fluff Website/src/content/case-studies/"*.md | wc -l && cat "/Users/andydoyle/Projects/Zero Fluff Website/src/content/content.config.ts"</automated>
  </verify>
  <done>Content collection config exists with case-studies schema. Two markdown files exist with frontmatter (title, client, result) and Problem/Approach/Outcome sections.</done>
</task>

<task type="auto">
  <name>Task 2: Case study listing page, individual pages, card component, and nav update</name>
  <files>src/components/CaseStudyCard.astro, src/pages/case-studies/index.astro, src/pages/case-studies/[id].astro, src/components/Header.astro</files>
  <action>
1. Create `src/components/CaseStudyCard.astro`:
   - Props: `id` (string), `title` (string), `client` (string), `result` (string)
   - Render as `<a href={/case-studies/${id}} class="card case-study-card stack">`
   - Display: `<h3>` with client name, `<p class="text-muted">` with title, `<p>` with result in bold and accent color
   - Hover styles: border-color transition to accent, subtle translateY(-2px) lift
   - Follow the code example from 03-RESEARCH.md exactly

2. Create `src/pages/case-studies/index.astro`:
   - `export const prerender = true`
   - Import BaseLayout, CaseStudyCard, getCollection from astro:content
   - Fetch all case studies with `getCollection('case-studies')`, sort by `data.order` (ascending, nulls last)
   - Page title "Our Work" (matches nav label)
   - Hero section: `<h1>` "Our Work", intro paragraph about real results for real businesses
   - Cards in `.grid-2` layout, each using CaseStudyCard component
   - Add `.reveal` class to sections for scroll animation
   - Straight-talking tone, no jargon

3. Create `src/pages/case-studies/[id].astro`:
   - `export const prerender = true`
   - Import BaseLayout, CTA, getCollection, render from astro:content
   - `getStaticPaths()` returning all case studies with `params: { id: study.id }` and `props: { study }`
   - Page layout: bold `<h1>` with client name, prominent result metric displayed large below the heading (use `var(--text-xl)` or similar, accent color), then rendered markdown content (Problem/Approach/Outcome sections)
   - Use `.container .narrow .stack` layout pattern for readable single-column
   - CTA at bottom with heading "Want results like these?" text "Let's talk about what AI can do for your business." buttonText "Get in Touch"
   - Add `.reveal` to content sections

4. Update `src/components/Header.astro`:
   - Add "Work" nav link between "Services" and "About": `<a href="/case-studies" class:list={[{ active: currentPath.startsWith('/case-studies') }]}>Work</a>`
   - Use `startsWith` for active state so it highlights on both listing and individual pages
  </action>
  <verify>
    <automated>cd "/Users/andydoyle/Projects/Zero Fluff Website" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build succeeds. /case-studies listing page renders 2 cards. /case-studies/apex-logistics and /case-studies/bright-path-digital pages generate with Problem/Approach/Outcome content. Header nav shows "Work" link.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors (all pages generate)
- `ls dist/case-studies/` shows index.html plus subdirectories for each case study
- Header nav contains 5 links including "Work"
- Case study pages contain Problem/Approach/Outcome sections and CTA
</verification>

<success_criteria>
- /case-studies listing shows 2 project cards with client name and result metric
- Clicking a card goes to /case-studies/{id} with Problem/Approach/Outcome structure
- Each case study displays a quantified result prominently
- CTA at bottom of each case study links to /contact
- "Work" appears in site navigation
- All pages prerender as static HTML
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-blog/03-01-SUMMARY.md`
</output>
