---
phase: 02-core-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/CTA.astro
  - src/components/Testimonial.astro
  - src/styles/global.css
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06

must_haves:
  truths:
    - "Landing on the homepage shows a bold headline about AI implementation above the fold"
    - "Scrolling reveals pain points section addressing why small businesses struggle with AI"
    - "A services summary section links to /services"
    - "Testimonials are displayed as social proof"
    - "A clear CTA directs visitors to /contact"
    - "All copy is direct, jargon-free, and sounds like a real person"
    - "Navigation includes links to all four pages: Home, Services, About, Contact"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Full homepage with hero, pain points, services summary, testimonials, CTA"
      min_lines: 80
    - path: "src/components/CTA.astro"
      provides: "Reusable call-to-action section component"
      exports: ["Props"]
    - path: "src/components/Testimonial.astro"
      provides: "Reusable testimonial quote block"
      exports: ["Props"]
    - path: "src/components/Header.astro"
      provides: "Updated navigation with all page links"
    - path: "src/components/Footer.astro"
      provides: "Footer with contact email and location"
    - path: "src/styles/global.css"
      provides: "Grid utility classes for card layouts"
      contains: ".grid-2"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/CTA.astro"
      via: "import and render"
      pattern: "import CTA from"
    - from: "src/pages/index.astro"
      to: "src/components/Testimonial.astro"
      via: "import and render"
      pattern: "import Testimonial from"
    - from: "src/components/Header.astro"
      to: "/services, /about, /contact"
      via: "nav anchor hrefs"
      pattern: "href=./services"
---

<objective>
Build the full homepage content and shared components (CTA, Testimonial) that all subsequent pages will reuse. Update site navigation and footer to include all four pages.

Purpose: The homepage is the first impression -- visitors must immediately understand what Zero Fluff does and feel confident enough to explore further. Shared components avoid duplication across pages.
Output: Complete homepage with hero, pain points, services summary, testimonials, and CTA. Reusable CTA.astro and Testimonial.astro components. Updated Header and Footer.
</objective>

<execution_context>
@/Users/andydoyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andydoyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pages/02-RESEARCH.md

@src/styles/global.css
@src/layouts/BaseLayout.astro
@src/components/Header.astro
@src/components/Footer.astro
@src/components/BaseHead.astro
@src/pages/index.astro

<interfaces>
<!-- Existing design system patterns (from Phase 1) the executor should use directly -->

From src/styles/global.css:
```css
/* Layout: .container .narrow .stack .stack-lg .section */
/* Typography: .text-lg .text-sm .text-muted */
/* Animation: .reveal (add to sections for scroll fade-in) */
/* Button: .btn (blue accent, hover lift) */
/* Spacing tokens: --space-xs through --space-2xl */
/* Type tokens: --text-sm through --text-3xl */
/* Color tokens: --color-bg, --color-text, --color-text-muted, --color-accent, --color-border */
```

From src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title?: string;
  description?: string;
}
// Includes Header, Footer, ClientRouter, scroll-reveal observer
```

From src/components/BaseHead.astro:
```typescript
interface Props {
  title?: string;
  description?: string;
}
// Defaults: title = 'Zero Fluff - AI Implementation That Works'
// Defaults: description = 'Straight-talking AI consultancy...'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared components, update navigation, and add CSS grid utilities</name>
  <files>src/components/CTA.astro, src/components/Testimonial.astro, src/components/Header.astro, src/components/Footer.astro, src/styles/global.css</files>
  <action>
**CTA.astro** -- Create reusable call-to-action section component following the exact pattern from the research doc:
- Props: `heading` (default "Ready to cut the fluff?"), `text` (default "Get in touch for a straight conversation about what AI can do for your business."), `buttonText` (default "Get in Touch"), `buttonHref` (default "/contact")
- Markup: `section.section.reveal > div.container.narrow.stack > h2 + p.text-lg + div > a.btn`
- Center-align the CTA text using `text-align: center` scoped style

**Testimonial.astro** -- Create reusable testimonial component following the research doc pattern:
- Props: `quote: string`, `name: string`, `role?: string`, `company?: string`
- Markup: blockquote with left accent border, italic quote text at `--text-lg`, attribution with name bold and role/company muted
- Use scoped styles from the research doc code example (border-left 3px solid accent, padding-left md)

**Header.astro** -- Update navigation to include all four pages:
- Add "Services" link to `/services` and "Contact" link to `/contact`
- Keep existing active-state logic using `currentPath` matching
- Add `/services` and `/contact` pattern matching (include trailing slash variants)

**Footer.astro** -- Add contact details:
- Add Andy's email (andy@zerofluff.co.uk) and location (Totnes, Devon) below the tagline
- Keep existing tagline and copyright
- Style the contact info with `text-sm text-muted`

**global.css** -- Add grid utility classes after the existing utility classes section:
```css
.grid-2 { display: grid; gap: var(--space-lg); }
@media (min-width: 768px) { .grid-2 { grid-template-columns: repeat(2, 1fr); } }
.grid-3 { display: grid; gap: var(--space-lg); }
@media (min-width: 768px) { .grid-3 { grid-template-columns: repeat(2, 1fr); } }
@media (min-width: 1024px) { .grid-3 { grid-template-columns: repeat(3, 1fr); } }
```
Also add a `.card` utility for pain points and services summary cards:
```css
.card { padding: var(--space-md); border: 1px solid var(--color-border); border-radius: 0.5rem; background: white; }
```
  </action>
  <verify>
    <automated>cd "/Users/andydoyle/Projects/Zero Fluff Website" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>CTA and Testimonial components exist with typed Props interfaces. Header shows Home, Services, About, Contact links. Footer shows email and location. global.css has .grid-2, .grid-3, .card utilities. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Build full homepage with all content sections</name>
  <files>src/pages/index.astro</files>
  <action>
Replace the current design-system test page (index.astro) with the real homepage. Import CTA and Testimonial components. Use BaseLayout with title "Zero Fluff - AI Implementation That Works".

**Section 1: Hero (no .reveal -- visible immediately)**
- `section.section > div.container.narrow.stack`
- h1: "AI Implementation That Actually Works" (bold, --text-3xl via h1)
- p.text-lg.text-muted: 2-3 sentences establishing what Zero Fluff does -- straight-talking AI consultancy for small businesses, no jargon, just results
- a.btn linking to /contact: "Let's Talk"

**Section 2: Pain Points (.reveal, alternate background)**
- `section.section.reveal` with subtle background `style="background-color: var(--color-border);"`
- div.container.stack-lg
- h2: "Sound familiar?" with p.text-muted subtitle
- div.grid-3 with 3 cards (div.card.stack) addressing common AI struggles:
  1. "You've seen the hype but don't know where to start" -- businesses overwhelmed by AI marketing
  2. "You tried ChatGPT but it didn't stick" -- tools without integration into real workflows
  3. "You're worried about wasting money on something that won't work" -- fear of bad investment
- Each card: h3 (short title) + p (2-3 sentences expanding, conversational tone)

**Section 3: Services Summary (.reveal)**
- `section.section.reveal > div.container.stack-lg`
- div.narrow.stack: h2 "What I Do" + p.text-lg intro
- div.grid-2 with 2-3 service cards summarising key offerings:
  1. "AI Workflow Automation" -- automating repetitive tasks using tools like N8N
  2. "Hands-On Implementation" -- building and deploying, not just advising
  3. (Optional 3rd) "Ongoing Support" -- keeping things running and adapting
- Each card: h3 + p (2-3 sentences)
- After the grid: p with link "See everything I offer" linking to /services (use a styled text link, not btn)

**Section 4: Social Proof / Testimonials (.reveal, alternate background)**
- `section.section.reveal` with background
- div.container.stack-lg
- div.narrow.stack: h2 "What People Say" (or similar)
- div.grid-2 (or stacked) with 2-3 Testimonial components
- Use realistic placeholder testimonials in the Zero Fluff voice. Examples:
  - Quote about saving hours per week on manual reporting, Name: "Sarah Mitchell", Role: "Operations Director", Company: "Apex Logistics"
  - Quote about finally understanding what AI can actually do for their business, Name: "James Cooper", Role: "Founder", Company: "Bright Path Digital"
- Mark these as placeholders with an HTML comment: `<!-- Placeholder testimonials -- replace with real ones -->`

**Section 5: CTA (.reveal)**
- Use the CTA component with defaults (heading: "Ready to cut the fluff?", links to /contact)

**Copy guidelines (HOME-06):**
- Write as Andy talking directly to the visitor. Use "I" and "you", not "we" and "our clients"
- No buzzwords: avoid "leverage", "synergy", "transform", "revolutionize", "cutting-edge"
- Short sentences. Active voice. Occasional question to the reader.
- Tone: confident but not arrogant, practical, slightly informal, like a knowledgeable friend giving advice
  </action>
  <verify>
    <automated>cd "/Users/andydoyle/Projects/Zero Fluff Website" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Homepage has 5 distinct sections (hero, pain points, services summary, testimonials, CTA). Hero visible immediately, other sections use .reveal. Pain points use .grid-3 with cards. Testimonial components render with placeholder quotes. CTA links to /contact. All copy is direct, jargon-free, first-person. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `npm run preview` serves the site -- homepage loads with all 5 sections visible on scroll
3. Navigation shows Home, Services, About, Contact links
4. Footer shows email and location
5. CTA buttons link to /contact
6. Services summary links to /services
</verification>

<success_criteria>
- Homepage renders with hero, pain points (3 cards), services summary, testimonials, and CTA
- All copy reads as straight-talking, zero-jargon, first-person
- Shared CTA and Testimonial components exist and are importable
- Navigation updated for all 4 pages
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-01-SUMMARY.md`
</output>
