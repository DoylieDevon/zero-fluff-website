---
phase: 04-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BaseHead.astro
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - src/pages/about.astro
  - src/pages/services.astro
  - src/pages/contact.astro
  - src/pages/blog.astro
  - src/pages/404.astro
  - astro.config.mjs
  - public/robots.txt
  - public/og-image.png
  - public/site.webmanifest
  - public/favicon.ico
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-05

must_haves:
  truths:
    - "Every page has a unique title, meta description, and OpenGraph tags in the HTML head"
    - "Sharing any page URL on LinkedIn/Twitter shows the site name, page title, description, and OG image"
    - "Viewing page source on the homepage reveals LocalBusiness JSON-LD structured data"
    - "sitemap-index.xml exists at the site root listing all public pages"
    - "robots.txt allows all crawlers and references the sitemap"
    - "Visiting a non-existent URL shows a branded 404 page with a link home"
    - "No cookie banner is present (no analytics at launch)"
  artifacts:
    - path: "src/components/BaseHead.astro"
      provides: "OG meta tags, Twitter card meta, canonical URL, favicon links, JSON-LD slot"
      contains: "og:title"
    - path: "astro.config.mjs"
      provides: "Sitemap integration"
      contains: "@astrojs/sitemap"
    - path: "public/robots.txt"
      provides: "Crawler directives"
      contains: "Sitemap:"
    - path: "src/pages/404.astro"
      provides: "Custom 404 page"
      contains: "prerender = true"
    - path: "public/og-image.png"
      provides: "Default OpenGraph image 1200x630"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/BaseHead.astro"
      via: "Props passthrough (title, description, image)"
      pattern: "BaseHead.*title.*description"
    - from: "src/pages/index.astro"
      to: "BaseLayout"
      via: "Unique title and description props"
      pattern: "title=.*description="
    - from: "astro.config.mjs"
      to: "dist/sitemap-index.xml"
      via: "@astrojs/sitemap integration"
      pattern: "sitemap"
---

<objective>
Add SEO metadata, structured data, sitemap, favicon set, OG image, robots.txt, and custom 404 page to make the site discoverable by search engines and shareable on social platforms.

Purpose: Every page needs unique meta tags and OG data for search visibility and social sharing. The sitemap and structured data help search engines understand and index the site. The 404 page keeps visitors on-brand when hitting dead links.

Output: Extended BaseHead with OG/Twitter meta and favicon links, sitemap integration, LocalBusiness JSON-LD on homepage, robots.txt, OG image, favicon set, custom 404 page.
</objective>

<execution_context>
@/Users/andydoyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andydoyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch-readiness/04-CONTEXT.md
@.planning/phases/04-launch-readiness/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/BaseHead.astro (current):
```astro
interface Props {
  title?: string;
  description?: string;
}
```

From src/layouts/BaseLayout.astro (current):
```astro
interface Props {
  title?: string;
  description?: string;
}
```

From astro.config.mjs (current):
```javascript
import { defineConfig, envField } from 'astro/config';
import vercel from '@astrojs/vercel';
export default defineConfig({
  site: 'https://zerofluff.co.uk',
  output: 'server',
  adapter: vercel(),
  env: { schema: { RESEND_API_KEY: envField.string({ context: 'server', access: 'secret' }) } },
});
```

From src/components/Footer.astro (current):
```astro
<nav class="footer-nav text-sm text-muted">
  <a href="/blog">Blog</a>
</nav>
```

Existing pages and their current title/description props:
- index.astro: title="Zero Fluff - AI Implementation That Works" (no description)
- about.astro: title="About - Zero Fluff", description="Meet Andy Doyle..."
- services.astro: title="Services - Zero Fluff", description="AI implementation services..."
- contact.astro: title="Contact - Zero Fluff", description="Get in touch about AI..."
- blog.astro: title="Blog" (no description)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEO metadata infrastructure -- BaseHead, BaseLayout, sitemap, robots.txt, favicon, OG image</name>
  <files>
    src/components/BaseHead.astro,
    src/layouts/BaseLayout.astro,
    astro.config.mjs,
    public/robots.txt,
    public/og-image.png,
    public/site.webmanifest,
    public/favicon.ico
  </files>
  <action>
1. **Install @astrojs/sitemap:** Run `npx astro add sitemap --yes`. If the interactive prompt blocks, manually: `npm install @astrojs/sitemap` then add the import and integration to astro.config.mjs.

2. **Update astro.config.mjs:** Add sitemap integration with a filter excluding the 404 page:
   ```javascript
   import sitemap from '@astrojs/sitemap';
   // In integrations array:
   sitemap({ filter: (page) => page !== 'https://zerofluff.co.uk/404/' })
   ```

3. **Extend BaseHead.astro Props interface** to add `image` prop:
   ```
   interface Props {
     title?: string;
     description?: string;
     image?: string;
   }
   ```
   Default `image` to `'/og-image.png'`. Construct `canonicalURL` from `new URL(Astro.url.pathname, Astro.site)` and `imageURL` from `new URL(image, Astro.site)`.

4. **Add to BaseHead.astro** after existing meta tags:
   - `<link rel="canonical" href={canonicalURL} />`
   - OpenGraph meta: og:type ("website"), og:url, og:title, og:description, og:image, og:site_name ("Zero Fluff"), og:locale ("en_GB")
   - Twitter meta: twitter:card ("summary_large_image"), twitter:title, twitter:description, twitter:image
   - Favicon links: `<link rel="icon" href="/favicon.ico" sizes="32x32" />`, keep existing favicon.svg link with type="image/svg+xml", add `<link rel="apple-touch-icon" href="/apple-touch-icon.png" />`, add `<link rel="manifest" href="/site.webmanifest" />`
   - Use the exact patterns from 04-RESEARCH.md Pattern 1

5. **Update BaseLayout.astro Props** to include `image?: string` and pass it through to BaseHead.

6. **Create public/robots.txt:**
   ```
   User-agent: *
   Allow: /

   Sitemap: https://zerofluff.co.uk/sitemap-index.xml
   ```

7. **Create public/og-image.png:** Generate a simple 1200x630px image. Use a Node.js script or Canvas to create a solid brand-coloured background with "Zero Fluff" in Space Grotesk and the tagline "AI Implementation That Works" below it. Keep it minimal -- typography on solid background matches the whitespace-heavy design. If programmatic generation is too complex, create a simple SVG and convert it, or create a placeholder PNG at the correct dimensions. The image should look good on LinkedIn.

8. **Create public/site.webmanifest** with the structure from research (name: "Zero Fluff", short_name: "Zero Fluff", theme_color and background_color matching site design).

9. **Create public/favicon.ico:** For now, the existing public/favicon.svg serves as the primary icon. Create a basic favicon.ico (can be a 32x32 version). If the user's source image is not yet available, note this with a comment in the webmanifest. The apple-touch-icon.png and android-chrome PNGs can be placeholder files or omitted with a TODO note -- the user will supply a source image for realfavicongenerator.net later per CONTEXT.md decisions.

IMPORTANT: Do NOT add a cookie banner or any analytics code. SEO-05 is N/A per user decision (no analytics at launch).
  </action>
  <verify>
    <automated>cd "/Users/andydoyle/Projects/Zero Fluff Website" && npx astro build 2>&1 | tail -20 && test -f dist/sitemap-index.xml && echo "SITEMAP OK" && test -f dist/robots.txt && echo "ROBOTS OK" && grep -q "og:title" dist/index.html && echo "OG TAGS OK" && grep -q "canonical" dist/index.html && echo "CANONICAL OK"</automated>
  </verify>
  <done>
    - BaseHead emits OG, Twitter, and canonical meta tags for every page
    - Favicon links reference ico, svg, apple-touch-icon, and webmanifest
    - sitemap-index.xml generated at build time listing all public pages (excluding 404)
    - robots.txt exists in public/ with sitemap reference
    - OG image exists at public/og-image.png at 1200x630
    - site.webmanifest exists in public/
    - No cookie banner or analytics code present
  </done>
</task>

<task type="auto">
  <name>Task 2: Structured data, page meta descriptions, and custom 404</name>
  <files>
    src/pages/index.astro,
    src/pages/about.astro,
    src/pages/services.astro,
    src/pages/contact.astro,
    src/pages/blog.astro,
    src/pages/404.astro
  </files>
  <action>
1. **Add LocalBusiness JSON-LD to the homepage (index.astro):** Insert a `<script type="application/ld+json" set:html={JSON.stringify({...})} />` tag inside the page content (or in the head via a slot if available). Use the exact schema from 04-RESEARCH.md Pattern 2:
   - @context: "https://schema.org"
   - @type: "LocalBusiness"
   - name: "Zero Fluff"
   - description: "Straight-talking AI consultancy for small businesses"
   - url: "https://zerofluff.co.uk"
   - address: PostalAddress with addressLocality "Totnes", addressRegion "Devon", addressCountry "GB"
   - founder: Person, name "Andy Doyle"
   - areaServed: "GB"
   - serviceType: ["AI Implementation", "AI Consultancy", "Business Automation"]

   Since the JSON-LD should be in the `<head>`, and BaseLayout puts BaseHead in the head, consider adding a `<slot name="head" />` to BaseLayout's head section so pages can inject head-level content. Alternatively, place the JSON-LD script at the top of the page body -- Google parses JSON-LD from anywhere in the document. Use whichever approach is simpler.

2. **Ensure all pages pass unique title and description to BaseLayout.** Review and update:
   - index.astro: title="Zero Fluff - AI Implementation That Works", description="Straight-talking AI consultancy helping small businesses implement AI that actually works. No jargon, no fluff, just results." (add description if missing)
   - blog.astro: title="Blog - Zero Fluff", description="Thoughts on AI implementation, automation, and making technology work for small businesses." (fix title format, add description)
   - about.astro, services.astro, contact.astro: already have both -- verify format consistency (all should follow "Page - Zero Fluff" pattern except homepage)

3. **Create src/pages/404.astro:**
   - Add `export const prerender = true` (required by Astro for 404 pages)
   - Use BaseLayout with title="Page Not Found - Zero Fluff"
   - On-brand straight-talking message. Example tone: "Well, this is awkward. The page you're looking for doesn't exist. It might have been moved, or maybe the URL has a typo. Either way, let's get you back on track."
   - Include a prominent link back to the homepage using the site's existing button/CTA styling
   - Keep it brief and helpful -- this is a brand personality opportunity per CONTEXT.md specifics
   - Use the established `.container .narrow .stack` layout classes
  </action>
  <verify>
    <automated>cd "/Users/andydoyle/Projects/Zero Fluff Website" && npx astro build 2>&1 | tail -10 && grep -q "application/ld+json" dist/index.html && echo "JSON-LD OK" && grep -q "LocalBusiness" dist/index.html && echo "LOCALBUSINESS OK" && test -f dist/404.html && echo "404 PAGE OK" && grep -q "og:description" dist/about/index.html && echo "ABOUT META OK" && grep -q "og:description" dist/blog/index.html && echo "BLOG META OK"</automated>
  </verify>
  <done>
    - Homepage contains LocalBusiness JSON-LD with correct business details
    - Every page has a unique title following "Page - Zero Fluff" pattern (homepage is the exception)
    - Every page has a unique meta description and OG description
    - 404.astro exists with prerender=true, on-brand message, and link to homepage
    - Build completes successfully with no errors
  </done>
</task>

</tasks>

<verification>
Build the site and verify:
1. `npx astro build` succeeds with no errors
2. `dist/sitemap-index.xml` exists and references a sitemap listing all pages (excluding 404)
3. Every page's HTML contains og:title, og:description, og:image, twitter:card, and canonical link
4. Homepage HTML contains `application/ld+json` with LocalBusiness schema
5. `dist/404.html` exists with on-brand content
6. `dist/robots.txt` exists with sitemap reference
7. No cookie banner or analytics scripts in any page
</verification>

<success_criteria>
- Build passes cleanly
- Every page has unique title + description + OG tags + canonical URL
- LocalBusiness structured data present on homepage
- sitemap-index.xml generated excluding 404
- robots.txt present with sitemap link
- Custom 404 page renders with BaseLayout
- OG image exists at correct dimensions
- No analytics or cookie consent code present (SEO-05 confirmed N/A)
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-readiness/04-01-SUMMARY.md`
</output>
