---
phase: 04-launch-readiness
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/actions/index.ts
autonomous: false
requirements:
  - DEPL-02
  - DEPL-03

user_setup:
  - service: vercel
    why: "Custom domain configuration"
    env_vars: []
    dashboard_config:
      - task: "Add zerofluff.co.uk as custom domain"
        location: "Vercel Dashboard -> Project -> Settings -> Domains"
  - service: godaddy
    why: "DNS records pointing to Vercel and Resend"
    env_vars: []
    dashboard_config:
      - task: "Set A record @ -> 76.76.21.21 (confirm IP in Vercel dashboard)"
        location: "GoDaddy DNS Management"
      - task: "Set CNAME www -> cname.vercel-dns.com."
        location: "GoDaddy DNS Management"
      - task: "Add Resend SPF, DKIM, MX records"
        location: "GoDaddy DNS Management (values from Resend dashboard)"
  - service: resend
    why: "Domain verification for production email"
    env_vars: []
    dashboard_config:
      - task: "Add zerofluff.co.uk domain and get DNS record values"
        location: "Resend Dashboard -> Domains -> Add Domain"
      - task: "Update from address to andy@zerofluff.co.uk after verification"
        location: "Update contact form action code"

must_haves:
  truths:
    - "Visiting https://zerofluff.co.uk loads the site with no certificate warnings"
    - "Visiting http://zerofluff.co.uk redirects to HTTPS"
    - "Visiting www.zerofluff.co.uk redirects to apex domain"
    - "Contact form sends email from andy@zerofluff.co.uk (verified Resend domain)"
  artifacts:
    - path: "src/actions/index.ts"
      provides: "Contact form Resend send() call with production from address"
      contains: "andy@zerofluff.co.uk"
  key_links:
    - from: "GoDaddy DNS A record"
      to: "Vercel hosting"
      via: "A record @ -> Vercel IP"
      pattern: "76.76.21.21"
    - from: "GoDaddy DNS CNAME"
      to: "Vercel hosting"
      via: "CNAME www -> cname.vercel-dns.com"
      pattern: "cname.vercel-dns.com"
    - from: "src/actions/index.ts"
      to: "Resend API"
      via: "Resend send() with verified domain from address"
      pattern: "andy@zerofluff.co.uk"
---

<objective>
Configure the production domain, HTTPS, and Resend domain verification so the site is live at zerofluff.co.uk.

Purpose: The site needs to be reachable on its production domain with HTTPS enforced and email delivery working from the verified domain.

Output: Live site at https://zerofluff.co.uk with working contact form email from andy@zerofluff.co.uk.
</objective>

<execution_context>
@/Users/andydoyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andydoyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch-readiness/04-CONTEXT.md
@.planning/phases/04-launch-readiness/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add domain to Vercel and update Resend from address</name>
  <files>src/actions/index.ts</files>
  <action>
1. **Add custom domain in Vercel CLI:** Run `npx vercel domains add zerofluff.co.uk` to register the domain with the Vercel project. Also add `www.zerofluff.co.uk` and configure it to redirect to the apex domain.

   If the CLI approach is blocked by authentication, this will be handled in the checkpoint task below.

2. **Update the contact form Resend "from" address:** Once the user confirms Resend domain verification is complete, update the Resend send() call in `src/actions/index.ts` to use `andy@zerofluff.co.uk` as the "from" address (replacing the current `onboarding@resend.dev` or test sender). Also update the "to" address to `andy@zerofluff.co.uk` if it differs.

Note: The actual DNS record configuration at GoDaddy and Resend dashboard setup are manual steps covered in the checkpoint below.
  </action>
  <verify>
    <automated>cd "/Users/andydoyle/Projects/Zero Fluff Website" && grep -r "andy@zerofluff" src/actions/index.ts && echo "FROM ADDRESS UPDATED" || echo "CHECK: from address may still need updating after Resend verification"</automated>
  </verify>
  <done>Vercel domain configuration initiated, contact form from address in src/actions/index.ts updated to andy@zerofluff.co.uk</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify domain, HTTPS, and email configuration</name>
  <action>
All code changes for launch readiness are complete (SEO metadata, sitemap, structured data, privacy policy, 404 page, favicon). Domain and email configuration requires manual dashboard steps. Guide the user through the verification checklist below.
  </action>
  <verify>
    <automated>curl -sI https://zerofluff.co.uk 2>/dev/null | head -5 || echo "Domain not yet resolving -- DNS may still be propagating"</automated>
  </verify>
  <done>Domain is live at https://zerofluff.co.uk with HTTPS enforced, www redirecting to apex, and contact form sending from verified domain email</done>
  <what-built>
All code changes for launch readiness are complete (SEO metadata, sitemap, structured data, privacy policy, 404 page, favicon). Domain and email configuration requires manual dashboard steps.
  </what-built>
  <how-to-verify>
Complete these manual steps, then verify:

**Step 1: Vercel Domain Setup**
1. Go to Vercel Dashboard -> your zero-fluff-website project -> Settings -> Domains
2. Add `zerofluff.co.uk` as a custom domain
3. Note the IP address Vercel shows (likely 76.76.21.21) and CNAME target

**Step 2: GoDaddy DNS for Vercel**
1. Go to GoDaddy DNS Management for zerofluff.co.uk
2. Set A record: Name=@, Value=76.76.21.21 (or the IP from Step 1)
3. Set CNAME record: Name=www, Value=cname.vercel-dns.com.
4. Wait for DNS propagation (can take 1-48 hours)

**Step 3: Resend Domain Verification**
1. Go to Resend Dashboard -> Domains -> Add Domain -> enter zerofluff.co.uk
2. Copy the SPF TXT record, DKIM TXT record, and MX record values
3. Add all three records in GoDaddy DNS Management
4. Click "Verify" in Resend dashboard (may take time to propagate)

**Step 4: Verify Everything Works**
1. Visit https://zerofluff.co.uk -- site loads with HTTPS, no cert warnings
2. Visit http://zerofluff.co.uk -- redirects to HTTPS
3. Visit www.zerofluff.co.uk -- redirects to apex (zerofluff.co.uk)
4. Submit a test contact form -- email arrives from andy@zerofluff.co.uk
5. Check https://zerofluff.co.uk/sitemap-index.xml loads
6. Share a page URL on LinkedIn -- preview shows OG image and correct title/description
  </how-to-verify>
  <resume-signal>Type "approved" when the domain is live and verified, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
1. https://zerofluff.co.uk loads the site over HTTPS
2. HTTP and www both redirect to https://zerofluff.co.uk
3. Contact form email arrives from andy@zerofluff.co.uk
4. SSL certificate is valid with no warnings
</verification>

<success_criteria>
- Site accessible at https://zerofluff.co.uk
- HTTPS enforced, HTTP redirects
- www redirects to apex domain
- Contact form sends from verified domain email
- DNS fully propagated
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-readiness/04-03-SUMMARY.md`
</output>
